// Sidebar styling for VuePress 1.x
.sidebar {
  background-color: #ff4444; // Red background

  .sidebar-link {
    color: white; // White text

    &:hover {
      background-color: rgba(255, 255, 255, 0.1);
    }

    &.active {
      font-weight: bold;
      color: #fff;
    }
  }
}